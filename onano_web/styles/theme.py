import reflex as rx
from .colors import BRAND_DARK_BLUE, BRAND_LIGHT_BLUE, BRAND_GRADIENT
from .fonts import FONT_FAMILY

# Theme configuration
theme = rx.theme(
    appearance="dark",
    has_background=True,
    radius="large",
    accent_color="blue",
)

# Global styles (if any custom CSS is needed)
styles = {
    # Custom global styles here
}
